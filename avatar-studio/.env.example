# Anthropic API キー (https://platform.claude.com/ で取得) — 必須
# アバター設計・台本・運用設計の生成に使用します
ANTHROPIC_API_KEY=sk-ant-...

# 生成に使うモデル (任意・省略時は claude-opus-5)
# ANTHROPIC_MODEL=claude-sonnet-5

# ------------------------------------------------------------
# 入口のパスワード(APP_PASSWORD)
#
# みんなで共有する場合や、インターネットに公開する場合は必ず設定してください。
# 設定すると、お名前とパスワードを入れるログイン画面が出るようになります。
# お名前は画面右上に出るだけで、誰でも自由に決められます。
# 本番で未設定だと誰もログインできません。
# 自分のパソコンで自分だけが使う場合は、未設定のままで構いません。
#
# 以前の BASIC_AUTH_USER / BASIC_AUTH_PASSWORD からの移行中は、
# BASIC_AUTH_PASSWORD の値も同じ合言葉として受け付けます。
# ------------------------------------------------------------
APP_PASSWORD=

# OpenAI API キー (https://platform.openai.com/ で取得) — 任意
# 設定すると「アバターの見た目案」を画像で確認できるようになります
# OPENAI_API_KEY=sk-...

# 画像生成モデル (任意・省略時は gpt-image-2)
# OPENAI_IMAGE_MODEL=gpt-image-1

# ------------------------------------------------------------
# データベース接続文字列(必須)
#
# アカウントと、顧客に関わるデータ(ブランドキットなど)の置き場です。
# 顧客情報は個々のパソコンに置かない決まりのため、
# 本番(NODE_ENV=production)では未設定だと起動しません。
# ローカル開発のときだけ .data/accounts.json に落ちます。
#
# ツールごとにテーブル名の接頭辞が違うので、同じ Neon データベースを
# 使い回しても、ツールごとに使える人を分けられます。
# ------------------------------------------------------------
DATABASE_URL=
