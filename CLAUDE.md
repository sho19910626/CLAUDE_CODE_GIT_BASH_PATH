# 個人情報・顧客情報の扱い（最優先のルール）

守る対象は、顧客企業名・実績数値・営業リスト・担当者の連絡先。
新しいツールを作るときも、既存を直すときも、次を必ず守る。

## 1. 個々のハードには置かない

顧客データを、使う人のパソコンやブラウザに残さない。具体的には次を禁止する。

| 禁止 | 代わりにやること |
|---|---|
| `localStorage` / `sessionStorage` への保存 | サーバー API 経由で共有データベースへ |
| サーバーのディスクへのファイル保存（`.data/store.json` など） | 共有データベースへ。本番では `DATABASE_URL` 未設定なら起動時に止まる |
| 事務所の1台を立ち上げて共有する方式 | クラウド（Vercel + Neon）に置く |

保存先を移したら、**移行前に端末へ残っているコピーを消す処理**も必ず入れる
（営業コンソールの `localStorage.removeItem(SENT_KEY)` と、Insta Studio のブランドキットが例）。

例外は「その人自身の入力の控え」だけ。営業コンソールの差出人欄（自分の名前・電話・メール）は、
本人の端末に残しても顧客情報ではないので許す。

## 2. 閲覧できる人を管理する

- **1 人 1 アカウント。** 共有アカウントは作らない（誰が何をしたか辿れなくなる）
- アカウントは**管理者が画面から発行**する。パスワードは 12 文字以上
- 権限は **管理者** と **一般** の 2 つ
  - 一般: 閲覧と入力
  - 管理者: それに加えてアカウントの発行・停止・権限変更、データの書き出し
- **退職・異動はアカウントを停止**する。停止すると、開いている画面もすぐ見られなくなる
  （middleware が毎回アカウントの有効性を確かめている）
- 全員をその場でログアウトさせたいときは `APP_PASSWORD` を変える（署名の鍵なので全セッションが切れる）

## 3. 書き出しは管理者だけ

書き出したファイルは手元に残る＝ルール 1 に反する操作なので、次の条件でのみ許す。

- 管理者だけが実行できる（画面で隠すだけでなく、**サーバー側でも権限を確かめる**）
- 誰がいつ何件書き出したかを記録に残す
- 実装は `/api/admin/export` に集約する。画面側で手元のデータから
  ファイルを組み立てる書き方（`exportJson` を直接呼ぶなど）はしない

## 4. 記録を残す

ログイン、データの取り込み・削除、書き出し、アカウントの発行・停止を記録する。
管理画面（Indeed 運用代行は `/indeed/admin`、他は `/admin`）の「操作の記録」で管理者が確認できる。

---

# このリポジトリで新しいツールを作るときの決めごと

## ログインは「お名前 + 共通パスワード」で必ず付ける

社内の少人数で共有する前提のツールばかりなので、**入口の作り方は全ツールで統一する**。
新しくツールを作ったら、動くようになった時点で必ずこれを入れる。後回しにしない。

なぜ必要か:

- 生成系の API は、URL を知られただけで第三者に使われ、そのまま課金に直結する
- 画面の中身は顧客企業名・実績数値・営業リストなど、外に出せないものが多い

### 決まっている仕様

| 項目 | 決めごと |
|---|---|
| 環境変数 | `APP_PASSWORD`(署名の鍵) と `DATABASE_URL`(アカウントとデータの置き場) |
| 入力項目 | お名前(アカウント名)とパスワード。1 人 1 アカウント |
| アカウント | 管理者が画面から発行する。共有アカウントは作らない |
| セッション | HMAC-SHA256 で署名した Cookie。有効期限 12 時間。毎回アカウントの有効性も確認 |
| Cookie 名 | ツールごとに変える(`acct_session` / `insta_session` / `avatar_session`) |
| 未設定のとき | 本番(`NODE_ENV=production`)は誰も入れない。ローカル開発だけ素通し |

ブラウザ標準の Basic 認証は使わない。ログアウトできず、誰が使っているか画面に出せず、
見た目もツールから浮くため。以前使っていた `BASIC_AUTH_USER` / `BASIC_AUTH_PASSWORD` は
廃止した(移行中の互換として `BASIC_AUTH_PASSWORD` の値だけは読む)。

### 実装のひな形

`insta-studio/` と `avatar-studio/` に同じものが入っている。新しいツールでは、この 7 ファイルを
そのままコピーして Cookie 名とタイトルだけ変える。

```
lib/users.ts                 パスワードの保管形式(scrypt)と入力チェック
lib/accounts.ts              アカウントと監査記録の保管(共有データベース)
lib/auth.ts                  セッションの署名・検証、currentUser / currentAdmin
lib/auth-edge.ts             middleware 用の検証(署名 + アカウントが有効か)
lib/rate-limit.ts            ログインの総当たり対策
lib/safe-fetch.ts            外部URL取得の安全確認(SSRF 対策)
middleware.ts                全ページ・全API をログイン必須にする
app/api/auth/login/route.ts  ログイン(POST) / ログアウト(DELETE)
app/login/page.tsx           ログイン画面(最初の管理者づくりも兼ねる)
app/admin/page.tsx           アカウント管理(管理者だけ)
app/api/admin/users/route.ts アカウントの発行・停止・権限変更
components/LoginForm.tsx     入力フォーム
components/AdminPanel.tsx    アカウント管理の画面
components/SessionBar.tsx    画面右上の「◯◯ さん / ログアウト」
components/LogoutButton.tsx  その中のボタン
```

`app/layout.tsx` の `<body>` の先頭に `<SessionBar />` を置く。

### 一緒に必ず入れるもの

点検で見つかった穴はどれも「うっかり抜けやすい」ものなので、ひな形に含めてある。

| 何を | どこに | 抜けると何が起きるか |
|---|---|---|
| ログインの回数制限 | `app/api/auth/login/route.ts` | 1秒に80回以上試せてしまい、短いパスワードが破られる |
| 飛び先の制限 `safeNext()` | `components/LoginForm.tsx` | `/login?next=https://…` で外部の偽サイトへ送り込める |
| 各APIでのログイン確認 | 生成系の `route.ts` すべて | middleware の除外設定を1行いじると課金APIが開く |
| `safeFetch()` | 利用者のURLを取りに行く箇所すべて | 社内LANの別機器の画面を取ってこられる(SSRF) |
| 防御ヘッダー | `next.config.mjs` の `headers()` | 他サイトの iframe に埋め込まれる |

利用者の入力を `innerHTML` に混ぜるときは必ずエスケープし、`href` に入れる値は
`http://` `https://` で始まるものだけ通すこと(`javascript:` を弾くため)。

### 落とし穴 — middleware で署名まで検証すること

middleware は Edge ランタイムで動くため `node:crypto` が使えない。
これを理由に「Cookie があるかどうか」だけ見る実装にすると、
**それらしい値を手で作っただけで中身が見えてしまう**。

`lib/auth-edge.ts` は Web Crypto (`crypto.subtle`) で同じ HMAC を計算しており、
middleware から署名を検証できる。ここを省略しない。

静的ファイル(`public/` に置いた HTML など)も middleware を通るので、
この方式なら一緒に守られる。API ルート側で個別に確認する必要はない。

## 公開するとき

Vercel の Environment Variables に `APP_PASSWORD` を入れてから Redeploy する。
入れ忘れると本番で誰もログインできない(素通しにはならない)。

---

# 手順の書き方(README・画面の案内・説明の全部)

セットアップや操作の手順を書くときは、**必ず番号を振って、上から順に 1 つずつ**書く。
「〜してください」を並べただけの説明にしない。読む人は途中で迷子になる。

## 守ること

| 決めごと | 理由 |
|---|---|
| **手順に通し番号を振る**(「手順 3／7」のように全体の何番目かも出す) | あと何回で終わるかが分かると、途中でやめない |
| **1 手順 = 1 動作**。1 つの番号に 2 つのことを詰めない | 片方だけやって次に進む事故を防ぐ |
| **打つものはそのまま貼れる形で書く**。省略や「〜など」を混ぜない | 考えずにコピーできる状態にする |
| **各手順の最後に「✅ こうなれば成功」を書く** | できたかどうかを自分で判定できる。ここが無いと、失敗に気づかないまま次へ進む |
| **先に必要なものを全部集めさせてから、設定に入る** | 途中で API キーを取りに行くと、手が止まって戻れなくなる |
| **OS で違うところは両方書く**(Mac / Windows) | 事務所の PC は Windows。`cp` が動かないだけで止まる |
| **「よくある失敗」を表にして添える** | 空白・引用符・`#` の消し忘れが実際に一番多い |
| **最後に「うまくいかないとき」の表を置く**(症状 → 原因 → 対処) | 症状から引ければ、質問しなくても自分で直せる |

## 特に、設定ファイル(`.env`)の説明で必ず書くこと

`.env` は git に入れていないので GitHub 上に存在しない。
リンクを踏んで 404 を見た人が「壊れている」と誤解するため、次の 3 つを必ず書く。

1. **GitHub に無いのは正常であること**と、その理由(鍵が入るファイルだから)
2. 見本(`.env.example`)からコピーして**手元で作る**こと。コピーの命令を Mac / Windows 両方
3. **`.env` は起動時にしか読まれない**こと(直したら必ず起動し直す)

Vercel に公開する手順でも、**`.env` はアップロードされない**ため
Environment Variables に 1 つずつ入れる必要があることを明記する。

## 会話で説明するときも同じ

チャットで手順を答えるときも、番号を振って 1 手順 1 動作で書く。
文章で流さない。相手はその場で手を動かしている。
